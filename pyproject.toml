[tool.pytest.ini_options]
addopts = "-q"
pythonpath = [
    "backend/src",
    "backend/tests",
]
testpaths = ["backend/tests"]

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]