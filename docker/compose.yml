services:
  trajectory-builder:
    build:
      context: ..
      dockerfile: docker/trajectory-builder/Dockerfile
    environment:
      TRAJECTORY_DATA_DIR: /data
    ports:
      - "5173:80"
    volumes:
      - ../backend/tests/data/trajectories:/data
